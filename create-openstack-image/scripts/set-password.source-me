#!/bin/env bash

# To avoid being prompted for your password each time,
# write your password below and uncomment the line
if [ -z "$CI" ] && [ -z "$OS_PASSWORD" ]; then
    read -e -s -p "Please enter your OpenStack Password: " OS_PASSWORD
    export OS_PASSWORD
fi